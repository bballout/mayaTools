import maya.cmds as cmds
import maya.mel as mel

sel = cmds.ls(sl=True)[0]
shapes = cmds.listRelatives(type='shape')
for shape in shapes:
    cmds.select('%s.cv[*]'%shape)
    clu = cmds.cluster()
    pc = cmds.parentConstraint(sel,clu)
    cmds.delete(pc)
    cmds.delete(sel,ch=True)
